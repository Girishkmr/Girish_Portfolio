# CLAUDE.md

We are building a portfolio website as described in @REQUIREMENTS.html and @README.md 
Read those file for general architectural tasks or to double-check the 
database structure, tech stack, application architecture, how many phase are complete etc. 
@REQUIREMENTS.html and @README.md  file should be updated when a major task or phase is completed along with github commit.

## docs/ must be kept current

`docs/` explains this system to its owner — architecture, data model, tech-stack
rationale, bugs hit and how they were diagnosed, likely interview questions, and
a from-scratch primer on the underlying concepts.

**Update the affected doc in the same change that ships the code.** A feature
merged without touching its documentation is unfinished work. `docs/README.md`
carries a table of which file covers what.

This matters more than usual here: these docs exist so the owner can understand
and defend the project, including in interviews. A stale doc is worse than a
missing one — revising from a document that no longer matches the code means
confidently stating something false. If a doc cannot be updated properly in the
same change, say so rather than leaving it quietly wrong.

This file provided guidance to Claude Code (Claude.ai/code) when working with the code in this repository.

Keep your replies extremely concise and focus on conveying the key information. No unneccessary fluff, no long code snippets.
